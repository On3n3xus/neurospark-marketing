---
phase: 01-quick-wins
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/sections/Footer.tsx
  - src/app/layout.tsx
  - package.json
autonomous: true
requirements:
  - QW-01
  - QW-03
  - QW-04

must_haves:
  truths:
    - "Footer displays LinkedIn, Twitter/X, and Instagram SVG icons"
    - "Each social icon links to the correct Neurospark profile and opens in a new tab"
    - "Each social link has rel='noopener noreferrer' and a descriptive aria-label"
    - "Vercel Analytics component renders in root layout"
    - "Vercel Speed Insights component renders in root layout"
  artifacts:
    - path: "src/components/sections/Footer.tsx"
      provides: "Social links with inline SVG icons"
      contains: "aria-label"
    - path: "src/app/layout.tsx"
      provides: "Analytics and SpeedInsights components in body"
      exports: ["RootLayout"]
  key_links:
    - from: "src/app/layout.tsx"
      to: "@vercel/analytics/next"
      via: "import Analytics"
      pattern: "Analytics.*from.*@vercel/analytics"
    - from: "src/app/layout.tsx"
      to: "@vercel/speed-insights/next"
      via: "import SpeedInsights"
      pattern: "SpeedInsights.*from.*@vercel/speed-insights"
    - from: "src/components/sections/Footer.tsx"
      to: "https://linkedin.com/company/neurospark-marketing"
      via: "anchor href"
      pattern: "linkedin\\.com"
---

<objective>
Add social media icon links to the footer and wire Vercel Analytics + Speed Insights into the root layout.

Purpose: Social links make Neurospark discoverable across platforms and build trust. Analytics must be live before any blog traffic arrives so baseline data is captured from day one.
Output: Updated Footer.tsx with three inline SVG social icons; updated layout.tsx importing Analytics and SpeedInsights; both @vercel packages installed.
</objective>

<execution_context>
@/Users/nexus/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nexus/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Current Footer.tsx — executor modifies this file -->
```tsx
// src/components/sections/Footer.tsx (current)
export default function Footer() {
  return (
    <footer className="border-t border-border bg-surface px-6 py-6">
      <div className="max-w-6xl mx-auto flex items-center justify-between">
        <p className="text-[13px] text-text-tertiary">
          &copy; {new Date().getFullYear()} Neurospark Marketing
        </p>
        <p className="text-[13px] text-text-tertiary hidden sm:block">
          Full-Service Creative Agency
        </p>
      </div>
    </footer>
  );
}
```

<!-- Current layout.tsx body — executor adds Analytics and SpeedInsights inside <body> -->
```tsx
// src/app/layout.tsx (relevant excerpt)
export default function RootLayout({ children }) {
  return (
    <html lang="en" className={`${geistSans.variable} ${geistMono.variable}`}>
      <head>...</head>
      <body className="bg-background text-text-primary font-sans antialiased">
        {children}
      </body>
    </html>
  );
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install @vercel/analytics and @vercel/speed-insights</name>
  <files>package.json</files>
  <action>
    Run the following install command from the project root:

    ```bash
    npm install @vercel/analytics@^2.0.1 @vercel/speed-insights@^2.0.0
    ```

    These are runtime dependencies (not devDependencies). After install, verify both appear in package.json `dependencies`.
  </action>
  <verify>
    <automated>node -e "require('@vercel/analytics'); require('@vercel/speed-insights'); console.log('ok')" 2>/dev/null && echo 'PASS' || echo 'FAIL'</automated>
  </verify>
  <done>Both @vercel/analytics and @vercel/speed-insights appear in package.json dependencies and resolve correctly.</done>
</task>

<task type="auto">
  <name>Task 2: Add Analytics and SpeedInsights to root layout</name>
  <files>src/app/layout.tsx</files>
  <action>
    Import and render both components in `src/app/layout.tsx`. Add the imports at the top of the file alongside existing imports:

    ```tsx
    import { Analytics } from "@vercel/analytics/next";
    import { SpeedInsights } from "@vercel/speed-insights/next";
    ```

    Place both components as self-closing tags INSIDE `<body>`, AFTER `{children}`:

    ```tsx
    <body className="bg-background text-text-primary font-sans antialiased">
      {children}
      <Analytics />
      <SpeedInsights />
    </body>
    ```

    Important: Do NOT convert layout.tsx to a Client Component (`"use client"`). Both components are server-safe Client Component leaves — they add their own `"use client"` directive internally. The root layout stays a Server Component.
  </action>
  <verify>
    <automated>cd "/Users/nexus/Documents/Neurospark Marketing/neurospark-marketing" && npx tsc --noEmit 2>&1 | grep -E "layout|analytics|speed" | head -10 || echo "No TS errors in layout"</automated>
  </verify>
  <done>layout.tsx imports Analytics from @vercel/analytics/next and SpeedInsights from @vercel/speed-insights/next; both render inside &lt;body&gt; after {children}; file has no TypeScript errors.</done>
</task>

<task type="auto">
  <name>Task 3: Add social icon links to Footer</name>
  <files>src/components/sections/Footer.tsx</files>
  <action>
    Replace the contents of `src/components/sections/Footer.tsx` with the version below. Key requirements:
    - Three inline SVG icons: LinkedIn, Twitter/X, Instagram
    - Each wrapped in an `<a>` with `href`, `target="_blank"`, `rel="noopener noreferrer"`, and `aria-label`
    - Social URLs (use these exact URLs — update if Neurospark has confirmed handles):
      - LinkedIn: `https://www.linkedin.com/company/neurospark-marketing`
      - Twitter/X: `https://twitter.com/neurosparkco`
      - Instagram: `https://www.instagram.com/neurosparkmarketing`
    - Icons styled with `text-text-tertiary hover:text-text-primary transition-colors` Tailwind classes
    - Icons placed on the right side of the footer row, replacing the "Full-Service Creative Agency" text (or alongside it — keep copyright on the left, social icons on the right)
    - SVG dimensions: 20x20px, `fill="currentColor"`

    Use these exact SVG paths:

    **LinkedIn:**
    ```svg
    <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="currentColor" aria-hidden="true">
      <path d="M20.447 20.452h-3.554v-5.569c0-1.328-.027-3.037-1.852-3.037-1.853 0-2.136 1.445-2.136 2.939v5.667H9.351V9h3.414v1.561h.046c.477-.9 1.637-1.85 3.37-1.85 3.601 0 4.267 2.37 4.267 5.455v6.286zM5.337 7.433a2.062 2.062 0 0 1-2.063-2.065 2.064 2.064 0 1 1 2.063 2.065zm1.782 13.019H3.555V9h3.564v11.452zM22.225 0H1.771C.792 0 0 .774 0 1.729v20.542C0 23.227.792 24 1.771 24h20.451C23.2 24 24 23.227 24 22.271V1.729C24 .774 23.2 0 22.222 0h.003z"/>
    </svg>
    ```

    **Twitter/X:**
    ```svg
    <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="currentColor" aria-hidden="true">
      <path d="M18.244 2.25h3.308l-7.227 8.26 8.502 11.24H16.17l-4.714-6.231-5.401 6.231H2.744l7.73-8.835L1.254 2.25H8.08l4.713 6.231zm-1.161 17.52h1.833L7.084 4.126H5.117z"/>
    </svg>
    ```

    **Instagram:**
    ```svg
    <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="currentColor" aria-hidden="true">
      <path d="M12 2.163c3.204 0 3.584.012 4.85.07 3.252.148 4.771 1.691 4.919 4.919.058 1.265.069 1.645.069 4.849 0 3.205-.012 3.584-.069 4.849-.149 3.225-1.664 4.771-4.919 4.919-1.266.058-1.644.07-4.85.07-3.204 0-3.584-.012-4.849-.07-3.26-.149-4.771-1.699-4.919-4.92-.058-1.265-.07-1.644-.07-4.849 0-3.204.013-3.583.07-4.849.149-3.227 1.664-4.771 4.919-4.919 1.266-.057 1.645-.069 4.849-.069zM12 0C8.741 0 8.333.014 7.053.072 2.695.272.273 2.69.073 7.052.014 8.333 0 8.741 0 12c0 3.259.014 3.668.072 4.948.2 4.358 2.618 6.78 6.98 6.98C8.333 23.986 8.741 24 12 24c3.259 0 3.668-.014 4.948-.072 4.354-.2 6.782-2.618 6.979-6.98.059-1.28.073-1.689.073-4.948 0-3.259-.014-3.667-.072-4.947-.196-4.354-2.617-6.78-6.979-6.98C15.668.014 15.259 0 12 0zm0 5.838a6.162 6.162 0 1 0 0 12.324 6.162 6.162 0 0 0 0-12.324zM12 16a4 4 0 1 1 0-8 4 4 0 0 1 0 8zm6.406-11.845a1.44 1.44 0 1 0 0 2.881 1.44 1.44 0 0 0 0-2.881z"/>
    </svg>
    ```

    Final footer structure:
    ```tsx
    export default function Footer() {
      return (
        <footer className="border-t border-border bg-surface px-6 py-6">
          <div className="max-w-6xl mx-auto flex items-center justify-between">
            <p className="text-[13px] text-text-tertiary">
              &copy; {new Date().getFullYear()} Neurospark Marketing
            </p>
            <div className="flex items-center gap-4">
              <a
                href="https://www.linkedin.com/company/neurospark-marketing"
                target="_blank"
                rel="noopener noreferrer"
                aria-label="Neurospark Marketing on LinkedIn"
                className="text-text-tertiary hover:text-text-primary transition-colors"
              >
                {/* LinkedIn SVG */}
              </a>
              <a
                href="https://twitter.com/neurosparkco"
                target="_blank"
                rel="noopener noreferrer"
                aria-label="Neurospark Marketing on Twitter / X"
                className="text-text-tertiary hover:text-text-primary transition-colors"
              >
                {/* X SVG */}
              </a>
              <a
                href="https://www.instagram.com/neurosparkmarketing"
                target="_blank"
                rel="noopener noreferrer"
                aria-label="Neurospark Marketing on Instagram"
                className="text-text-tertiary hover:text-text-primary transition-colors"
              >
                {/* Instagram SVG */}
              </a>
            </div>
          </div>
        </footer>
      );
    }
    ```
  </action>
  <verify>
    <automated>cd "/Users/nexus/Documents/Neurospark Marketing/neurospark-marketing" && npx tsc --noEmit 2>&1 | grep "Footer" | head -5 || echo "No TS errors in Footer"</automated>
  </verify>
  <done>Footer.tsx renders three anchor tags containing inline SVGs. Each anchor has target="_blank", rel="noopener noreferrer", and a unique aria-label naming the platform. No TypeScript errors.</done>
</task>

</tasks>

<verification>
After all tasks complete, run a full build to confirm no regressions:

```bash
cd "/Users/nexus/Documents/Neurospark Marketing/neurospark-marketing" && npm run build 2>&1 | tail -20
```

Expected: Build completes successfully. No type errors. Both @vercel packages appear in the build output.
</verification>

<success_criteria>
1. `npm run build` exits 0 with no errors
2. Footer.tsx contains exactly 3 social anchor links with `target="_blank"`, `rel="noopener noreferrer"`, and `aria-label` on each
3. layout.tsx contains `import { Analytics } from "@vercel/analytics/next"` and `import { SpeedInsights } from "@vercel/speed-insights/next"` and renders both in `<body>`
4. No `"use client"` directive added to layout.tsx
</success_criteria>

<output>
After completion, create `.planning/phases/01-quick-wins/01-01-SUMMARY.md` following the summary template.
</output>
