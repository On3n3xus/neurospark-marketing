---
phase: 01-quick-wins
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .env.example
autonomous: true
requirements:
  - QW-02

must_haves:
  truths:
    - ".env.example exists at the repository root"
    - "Every process.env.* variable used in the codebase has an entry in .env.example"
    - "Each entry has a comment explaining what the variable does and where to get its value"
  artifacts:
    - path: ".env.example"
      provides: "Documentation for all environment variables"
      contains: "NEXT_PUBLIC_SITE_URL"
  key_links:
    - from: ".env.example"
      to: "src/app/layout.tsx"
      via: "NEXT_PUBLIC_SITE_URL documents the variable used on line 16"
      pattern: "NEXT_PUBLIC_SITE_URL"
---

<objective>
Create `.env.example` at the repository root documenting every `process.env.*` variable used anywhere in the codebase.

Purpose: Any developer (or Claude) cloning the project can immediately see what environment variables are required, what each one does, and where to obtain its value. Required as a definition-of-done gate before any subsequent phase.
Output: `.env.example` file at repo root with all current env vars documented with placeholder values and inline comments.
</objective>

<execution_context>
@/Users/nexus/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nexus/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- All process.env.* usages found in the codebase (audited 2026-03-24): -->

src/app/layout.tsx:16
  process.env.NEXT_PUBLIC_SITE_URL || "https://neurospark.agency"
  Purpose: Canonical site URL used for metadataBase and JSON-LD org schema

src/app/robots.ts:3
  process.env.NEXT_PUBLIC_SITE_URL || "https://neurospark.agency"
  Purpose: Same variable — base URL for robots.txt Allow directive

src/app/sitemap.ts:4
  process.env.NEXT_PUBLIC_SITE_URL || "https://neurospark.agency"
  Purpose: Same variable — base URL for sitemap entries

<!-- No other process.env.* usages exist in the codebase as of this audit. -->
<!-- Future phases (Phase 3 MDX Blog) may add NEXT_PUBLIC_VERCEL_URL or similar — -->
<!-- update .env.example in those phases' definition-of-done. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create .env.example with all documented environment variables</name>
  <files>.env.example</files>
  <action>
    Create `.env.example` at the project root (`/Users/nexus/Documents/Neurospark Marketing/neurospark-marketing/.env.example`) with the following exact contents:

    ```
    # =============================================================================
    # Neurospark Marketing — Environment Variables
    # =============================================================================
    # Copy this file to .env.local and fill in your values.
    # Never commit .env.local to version control.
    # =============================================================================

    # -----------------------------------------------------------------------------
    # Site URL
    # -----------------------------------------------------------------------------
    # The canonical public URL of the site (no trailing slash).
    # Used for: metadataBase in root layout (OG images, Twitter cards),
    #           robots.txt Allow directive, sitemap.xml entry URLs.
    # In Vercel production: set to your custom domain (e.g. https://neurospark.agency)
    # In Vercel preview: leave unset — the app falls back to "https://neurospark.agency"
    # Locally: set to http://localhost:3000 for correct absolute URL generation
    NEXT_PUBLIC_SITE_URL=http://localhost:3000
    ```

    Notes on format:
    - Use `VARIABLE=placeholder_value` (not `VARIABLE=""` or `VARIABLE=<description>`)
    - `http://localhost:3000` is the correct local dev placeholder for a URL variable
    - The comment block above each variable must explain: what it's for, where it's used, and where to get the value
    - Keep the file lightweight — one variable section for now, more added in future phases
  </action>
  <verify>
    <automated>test -f "/Users/nexus/Documents/Neurospark Marketing/neurospark-marketing/.env.example" && grep -q "NEXT_PUBLIC_SITE_URL" "/Users/nexus/Documents/Neurospark Marketing/neurospark-marketing/.env.example" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>.env.example exists at repo root, contains NEXT_PUBLIC_SITE_URL with a placeholder value and a comment describing its purpose and usage locations.</done>
</task>

</tasks>

<verification>
Verify the audit is complete — no process.env.* variables exist in the codebase that are missing from .env.example:

```bash
grep -rn "process\.env\." "/Users/nexus/Documents/Neurospark Marketing/neurospark-marketing/src" \
  --include="*.ts" --include="*.tsx" --include="*.js" --include="*.mjs" \
  | grep -v "node_modules"
```

Expected output: only `NEXT_PUBLIC_SITE_URL` references in layout.tsx, robots.ts, and sitemap.ts. If any new variables appear, add them to .env.example before marking this plan complete.
</verification>

<success_criteria>
1. `.env.example` exists at the repository root
2. File contains an entry for `NEXT_PUBLIC_SITE_URL` with placeholder value `http://localhost:3000`
3. Every `process.env.*` variable found by the grep audit above has a corresponding entry in `.env.example`
4. Each entry has an explanatory comment (what it does, where it's used, where to get it)
</success_criteria>

<output>
After completion, create `.planning/phases/01-quick-wins/01-02-SUMMARY.md` following the summary template.
</output>
